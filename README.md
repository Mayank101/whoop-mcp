# whoop-mcp


## Privacy

This is a personal, self-hosted tool. It runs on the user's own machine
using their own WHOOP developer credentials. No data is collected,
transmitted, or stored by the author. WHOOP data is fetched directly
from WHOOP's API to the user's local machine, and any food logs or notes
are stored in a local file on that machine.
